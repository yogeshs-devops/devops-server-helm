apiVersion: v1
kind: Service
metadata:
  name: {{ include "calc-app.fullname" . }}
  labels:
    {{- include "calc-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calc-app.selectorLabels" . | nindent 4 }}
